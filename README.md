# 才想好写什么

CrystalX Horizon 的个人博客。前台托管于 GitHub Pages，内容管理后台托管于 Sites。

- `index.html` / `style.css` / `app.js`：静态前台。
- `content.json`：已发布内容。草稿只保留在 Sites 云端数据库中。
- GitHub Pages 从 `main` 分支根目录发布。

署名：CrystalX Horizon
